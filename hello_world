#include<stdio.h>  // 전처리기기 지시문 : 컴파일 하기 전에 컴파일러에 컴파일할 때 필요한 것들을 알려주야 한다. 이때 사용하는 것이 전처리기 지시문.  # 을 붙여야 함.
                  // #includ : 지정한 파일을 코드에 포함하라는 뜻. (전처리기 지시문에서 가장 많이 쓰임.)   
				  // stdio.h : 입출력 함수를 포함한 헤더 파일. 상수, 변수, 함수, printf(), scanf() 등 입출력 관련 함수들이 선언 되어 있어 stdio.h를 적으면 따로 추가 작업 없어도 이러한 함수를 바로 사용 가능.
                  // *컴파일___컴퓨터 언어 -> 기계어 번역하는 과정 / 컴파일러___컴파일 역할을 하는 프로그램 (ex. 비쥬얼 스튜디오) 

int main() {         // int main(void){}는 main이란 이름의 함수 정의를 나타냄. 
	                //**main() 함수는 모든 프로그램의 시작점으로, 한 프로젝트 안에 ☆반드시 하나만 존재해야함☆

	printf("hello world");

	return 0;
}
